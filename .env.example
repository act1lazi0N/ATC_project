# Generate by: openssl rand -base64 32
JWT_SECRET=replace_this_with_your_generated_secret

# Spring profile: default, docker, or test
SPRING_PROFILES_ACTIVE=docker

# PostgreSQL (if you want to override the default values)
# POSTGRES_USER=transfer_user
# POSTGRES_PASSWORD=your_strong_password
